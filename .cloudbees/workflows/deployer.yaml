apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: deployer
on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
      manifest:
        type: string
        required: true

jobs:
  hackers-api:
    if: ${{ fromJSON(inputs.manifest)['hackersapp-may-api']['hackers-api'].deploy }}
    uses: cb-golden-demo-org/hackersapp-may-api/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['hackersapp-may-api']['hackers-api'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['hackersapp-may-api']['hackers-api'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-auth:
    if: ${{ fromJSON(inputs.manifest)['hackersapp-may-auth']['hackers-auth'].deploy }}
    uses: cb-golden-demo-org/hackersapp-may-auth/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['hackersapp-may-auth']['hackers-auth'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['hackersapp-may-auth']['hackers-auth'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-web:
    if: ${{ fromJSON(inputs.manifest)['hackersapp-may-web']['hackers-web'].deploy }}
    uses: cb-golden-demo-org/hackersapp-may-web/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['hackersapp-may-web']['hackers-web'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['hackersapp-may-web']['hackers-web'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
